name: medusajs_sdk_dart
description: A modular, typed Dart SDK for the Medusa API.
version: 0.0.1
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ^3.0.0

# Add regular dependencies here.
dependencies:
  dio: ^5.9.0
  json_annotation: ^4.9.0
  equatable: ^2.0.7

dev_dependencies:
  lints: ^3.0.0
  test: ^1.25.0
  build_runner: ^2.10.4
  json_serializable: ^6.11.3
